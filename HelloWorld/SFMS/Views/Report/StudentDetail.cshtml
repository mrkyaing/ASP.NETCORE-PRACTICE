@model IList<StudentReport>
@{
    ViewData["Title"] = "Student Detail Report";
}
<!-- Content Header (Page header) -->
<div class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1 class="m-0">Student Detail Report</h1>
            </div><!-- /.col -->
            <div class="col-sm-6">
                <ol class="breadcrumb float-sm-right">
                    <li class="breadcrumb-item"><a href="#">Reports</a></li>
                    <li class="breadcrumb-item active"><a href="#">Student Detail Report</a></li>
                </ol>
            </div><!-- /.col -->
        </div><!-- /.row -->
    </div><!-- /.container-fluid -->
</div>

<form action="/Report/StudentDetailFromCodeToCode" method="get" target="_blank">
    <div class="mb-3">
        <label class="form-label">From Code</label>
       <select name="FromCode" class="form-control">
            @foreach(var item in Model) {
                <option value="@item.Code">@item.Code</option>
            }
        </select>
    </div>
    <div class="mb-3">
        <label class="form-label">From Code</label>
        <select name="ToCode" class="form-control">
            @foreach (var item in Model) {
                <option value="@item.Code">@item.Code</option>
            }
        </select>
    </div>
    <div class="mb-3">
        <div class="col">
            <input type="submit" class="btn btn-success" value="Show">
            <input type="reset" class="btn btn-primary" value="Cancel">
        </div>
    </div>
</form>